# Versioning
Always update the app version in `index.html` and `fullhistory.html` (or any other HTML file with the footer) when making changes. The version string is located in `<footer class="app-footer">` and should be formatted exactly as `vMMDDYY-HHMM` (e.g. `v090326-1230`).
